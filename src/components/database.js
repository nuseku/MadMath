//Will hold the username and password database array that is used in login
//This file is ignored by .gitignore, which keeps the username and password confidential

//Database connection hidden by .gitignore

// const { Pool } = require("pg");

// const pool = new Pool({
//   host: "localhost",
//   user: "postgres",
//   password: "Phone2259731.",
//   database: "lunamathdb",
//   port: 5432,
// });

// module.exports = pool;

const database = [{ username: "miktad", password: "lunamath" }];

export default database;
